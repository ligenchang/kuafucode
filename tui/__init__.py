"""nvagent TUI package.

Primary entry point: ``launch_tui`` in ``nvagent.tui.repl`` — tries the
Textual two-panel app first, falls back to the ANSI REPL if textual is
unavailable or the terminal doesn't support it.
"""
